<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:name=".HalqaApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Halqa"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Halqa.Splash"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                NOTE: a dead Android App Links intent-filter for
                `https://halqa.app` was removed in 2025-11. Halqa has no
                /.well-known/assetlinks.json deployed, MainActivity does
                NOT process incoming Intents (no onNewIntent override, no
                getIntent().data parsing), and no nav route in HalqaApp
                is reachable through a URL. Re-introducing this filter
                MUST be paired with:
                  1. shipping a verified assetlinks.json under
                     https://halqa.app/.well-known/ (publish via the
                     halqa-backend Vercel project), AND
                  2. handling the inbound deep link in MainActivity
                     (forward intent.data into the Compose nav graph).
                Until both exist, leaving the filter declared lets any
                site send users into Halqa for an empty home view, and
                masks future deep-link bugs behind an "auto-verified"
                channel that never actually verifies.
            -->
        </activity>

        <!-- LiveKit publisher session keeps camera + microphone active while
             the app is backgrounded; foregroundServiceType matches the
             FOREGROUND_SERVICE_CAMERA and FOREGROUND_SERVICE_MICROPHONE
             permissions declared above (Android 14+ enforced). -->
        <service
            android:name=".livekit.LiveBroadcastService"
            android:exported="false"
            android:foregroundServiceType="camera|microphone" />
    </application>
</manifest>
